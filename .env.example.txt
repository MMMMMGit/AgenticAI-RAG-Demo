# .env.example
# Copy this file to .env and customize if needed

# FastAPI backend URL (change only if running on different host/port)
FASTAPI_HOST=http://127.0.0.1:8000

# Optional: Streamlit port (default 8501)
# STREAMLIT_SERVER_PORT=8501